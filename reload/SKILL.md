---
name: reload
description: 直前の自分の応答を一字一句変えずにそのまま再出力する。ターミナルの画面幅変更などで表示が崩れた時に使う。基本は `/reload` で明示的に呼ばれる。
---

# Reload

直前のアシスタント応答テキストを、そのまま再出力する。

- 前置き・後書き・要約・言い換え・改変は一切しない。一文字も変えない。
- tool call は再実行しない。テキストの再描画だけが目的。
